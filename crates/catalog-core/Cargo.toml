[package]
name = "catalog-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Minimal domain types for the federated catalog rewrite: node identity, crawl work items, and the Catalog/Dataset/DataService model."

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
